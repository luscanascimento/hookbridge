# ==============================================================================
# HOOKBRIDGE — PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================

# Application Ports
API_PORT=8080
WEB_PORT=80

# PostgreSQL Database Configuration
DB_NAME=hookbridge_db
DB_USER=postgres
DB_PASSWORD=your_secure_postgres_password_here
DB_PORT=5432

# RabbitMQ Message Broker
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=your_secure_rabbitmq_password_here
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672

# Redis Cache & Distributed State
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_PORT=6379

# JWT Authentication & Tokens (Minimum 32 characters / 256 bits)
JWT_SECRET_KEY=replace_with_cryptographically_secure_jwt_key_at_least_32_bytes_long!
JWT_ISSUER=HookBridge
JWT_AUDIENCE=HookBridgeClients
JWT_EXPIRATION_MINUTES=60
JWT_REFRESH_DAYS=7

# Webhook Encryption (256-bit AES-GCM Master Key: 64 hex chars or 32+ bytes)
WEBHOOK_ENCRYPTION_MASTER_KEY=replace_with_64_character_hex_master_key_or_32_bytes_secret

# EventFlow Integration (Data Plane)
EVENTFLOW_BASE_URL=http://eventflow-api:5000
EVENTFLOW_API_KEY=replace_with_eventflow_data_plane_api_key

# OpenTelemetry / Tracing Collector
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317
OTEL_SERVICE_NAME=HookBridge.ControlPlane
